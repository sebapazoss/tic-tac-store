# URL base de la API REST (backend). 
# En Vercel, si usas el backend en la misma cuenta y el ruteo de Laravel con index.php,
# recuerda que podría requerir el doble prefijo /api/api como detalla la documentación.
# Ejemplo: https://tu-backend-dominio.com/api/api o https://tu-backend-dominio.com/api
VITE_API_BASE_URL=
